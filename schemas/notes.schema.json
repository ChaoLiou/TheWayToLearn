{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "notes.json — 這一站留給我的觀念／技巧，/learn-notes 由 agent 依 rules/notes.md 產生",
  "type": "object",
  "required": ["video_id", "notes"],
  "properties": {
    "video_id": { "type": "string" },
    "created": { "type": "string", "description": "ISO 時間，notes.py 排序用；沒有就看檔案 mtime" },
    "notes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "items": {
        "type": "object",
        "required": ["id", "seg_id", "kind", "text"],
        "properties": {
          "id": { "type": "integer", "minimum": 1, "description": "站內流水號，keep/drop 狀態靠它對" },
          "seg_id": { "type": "integer", "minimum": 1, "description": "出自 analysis.json 的哪一段；連回 plan.html#<vid>-s<seg_id>" },
          "kind": { "enum": ["concept", "skill", "insight"], "description": "concept = 觀念（是什麼、為什麼）；skill = 技巧（怎麼做、可直接用）；insight = 體悟（心態或判斷準則）" },
          "text": { "type": "string", "minLength": 4, "maxLength": 80, "description": "一句話，我自己看得懂、不必回頭看影片" },
          "detail": { "type": "string", "maxLength": 240, "description": "選填：為什麼重要／怎麼用，1–2 句" },
          "terms": { "type": "array", "items": { "type": "string" }, "description": "選填：牽涉的術語（英文原文）" }
        }
      }
    }
  }
}
