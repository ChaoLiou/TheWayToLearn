[project]
name = "thewaytolearn"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "yt-dlp>=2025.1.0",
  "pyyaml>=6",
  "jinja2>=3",
  "jsonschema>=4",
  "edge-tts>=7.2.8",
]

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.5"]

[tool.ruff]
line-length = 100
